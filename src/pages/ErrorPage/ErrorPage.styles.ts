import { styled } from "@mui/material";

export const ErrorPageContainer = styled("div")({
  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
  alignItems: "center",
});

export const ErrorPageActionsBox = styled("div")({
  display: "flex",
  alignItems: "center",
  marginTop: "1rem",
});
